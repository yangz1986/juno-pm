# Trust-Gap Mitigations · Juno

_Module 4. Generated by `M4 - AI-UX Trust Gap Checker.html`. Feature: _(name the feature)_. Paste over `04-ai-ux/trust-gaps.md`._

## Trust gaps

| Gap | Where it shows up | User cost | Mitigation |
|---|---|---|---|
| **The Black-box Gap** | Can the user see why the AI decided? | Mostly closed, residual risk (4/5 closed) | Every row already carries a mandatory Source ID citation (Slack link / Jira key / transcript timestamp) — that's a hard rule inherited from the system prompt, not new. What's still missing: the citation shows the evidence, not the scoring logic. Ship a "Why this rank?" expand on each row showing the severity×frequency inputs that produced the position (message count, account tier, source contradiction flags) |
| **The Hallucination Gap** | Could this confidently be wrong? | Mostly closed, residual risk (4/5 closed) | The <70%-confidence handoff and NEEDS CLARIFICATION rule already stop Juno from presenting a low-confidence guess as a ranked finding — that's the single strongest mitigation in this whole project. What's not yet spec'd: a visible confidence cue on-screen, not just a backend threshold. Add a dotted-border "low confidence" tag on any row near the 70% line, plus a per-row Regenerate/re-rank button so a PM can retry a specific row without re-running the whole table. |
| **The Control Gap** | Can the user steer or stop? | Mostly closed, residual risk (4/5 closed) | The earlier user-flow already specs a kill switch — inline "Not P0 / demote" and "Needs human review" buttons per row, which log a correction rather than firing an autonomous Jira/Slack write, preserving the system prompt's "never applies the change" rule. Residual gap: no "don't flag this pattern again" mute exists yet, so a recurring false positive (e.g., a noisy but low-severity integration) would keep resurfacing and eroding trust. Add a per-theme mute that suppresses future auto-posts of that pattern until a human re-enables it. |
| **The Intelligence Tax** | Is the latency / privacy / cognitive load worth the value? | Mostly closed, residual risk (4/5 closed) | p95≤4s is already set and the Processing State pillar already specs breadcrumbs ("Triaging this thread…" → "Cross-referencing…" → "Ranking…") so the wait reads as transparency, not a stall. Residual risk: nothing yet caps how often Juno proactively posts into a single thread — if severity/frequency shifts and Juno re-posts repeatedly, that's cognitive load stacking on top of latency. Cap auto-posts to one per thread per hour, and if that cap is regularly hit, treat it as a signal to review whether the threshold trigger itself is too sensitive. |

## Highest-priority fix

**The Black-box Gap** (4/5). Every row already carries a mandatory Source ID citation (Slack link / Jira key / transcript timestamp) — that's a hard rule inherited from the system prompt, not new. What's still missing: the citation shows the evidence, not the scoring logic. Ship a "Why this rank?" expand on each row showing the severity×frequency inputs that produced the position (message count, account tier, source contradiction flags)

## Verdict

**Shippable.** Trust posture passes the M4 readiness check. Confirm with eval data in M6 (golden set + guardrails).
